import { platform } from "os";

const authConfig = {
	providers: [
		{
			domain: "https://inspired-burro-62.clerk.accounts.dev",
			applicationID: "convex",
		},
	],
};
export default authConfig;
